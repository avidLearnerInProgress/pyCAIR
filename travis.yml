language: python

branches:
  only:
    - "master"

# In theory, older versions would probably work just fine
python:
  - 3.4

matrix:
  allow_failures:
    - python: 3.4
  fast_finish: true

env:
  - GOOS=windows

notifications:
email:
    on_success: never
    on_failure: never